# お金 セットアップ

アプリ本体: https://gogojoe312-max.github.io/okane/

## GAS（gogojoe312 の個人アカウントでやる）

1. https://script.google.com → 新しいプロジェクト（名前: お金）
2. `gas/code.gs` の中身を全部貼り付けて保存
3. 左の歯車（プロジェクトの設定）→ スクリプト プロパティ → 2つ追加
   - `ANTHROPIC_API_KEY` = AnthropicのAPIキー
   - `TOKEN` = 好きな合言葉（アプリ側にも同じものを入れる）
4. エディタ上部で関数 `setup` を選んで実行 → Gmail/スプレッドシートの権限を承認
   （「お金DB」というスプレッドシートが自動で作られる）
5. デプロイ → 新しいデプロイ → 種類: ウェブアプリ
   - 実行ユーザー: 自分 / アクセスできるユーザー: 全員
   - 出てきたURLをコピー
6. 左の時計（トリガー）→ トリガーを追加
   - 関数: `scanMail` / イベント: 時間主導型 / 分ベース / **15分おき**

## アプリ側

1. iPhoneのSafariで上のURLを開く → 共有 → ホーム画面に追加
2. 設定タブ → GAS Web App URL と合言葉を貼る → 接続テスト
3. 予算・給料日(25)を入れる

## 使い方メモ

- 入力バー: 「セブン 800円」→ 即記録 ／ 質問文 → AIが答える
- 📷: レシート撮影 → 確認して登録
- 銀行CSV: 設定 → 取込（Shift-JIS対応、列は自動判定）
- カード通知メールは15分毎に自動で入る（初回は権限承認後から）
